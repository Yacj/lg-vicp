---
description: 代码质量、配置清理与交付验证
globs: "**/*"
alwaysApply: true
---

# 质量门禁

- 修改既有文件前先理解其职责和当前未提交改动，不回退无关变更。
- `package.json` 与 `vite.config.ts` 允许在验证后清理已确认废弃的脚本、依赖、导入、插件或配置；禁止无依据删除或破坏现有功能。
- 新增外观枚举或持久化字段时必须同步默认值、运行时校验、导入导出和单元测试。
- 测试不得使用 Mock 冒充正式业务数据，只允许通用协议夹具和浏览器能力替身。
- 完成任务必须执行 `pnpm typecheck`、`pnpm test`、`pnpm build`。
- 验证失败必须修复本次变更新增的问题；不得跳过检查或删除测试来规避失败。