---
description: 蓝格后端常见需求修改位置
alwaysApply: true
---

- 项目创建、公开/私有或读取权限：项目模块、`src/shared/permissions.ts`、Drizzle schema。
- 用户、角色、权限、部门或字典：用户、权限、系统管理模块、Drizzle schema。
- 文件上传和下载：文件模块与 `src/storage`。
- PDF/DOCX 解析、OCR 和知识切片：文档 Worker、OCR 服务、知识模块。
- 知识库文档、版本、解析、检索、多来源接入（批量导入/爬虫/内部 API）和评测：`src/modules/knowledge/`、`knowledge_*`/`parsing_jobs` 表；内部 Structured Knowledge `knowledge_facts` + `knowledge-fact-resolver.ts`（优先 VERIFIED Fact，fallback Legacy Product）；B 端普通生命周期编排在 `knowledge-workflow.service.ts`。详见 `docs/knowledge/README.md`。
- 采集管理（独立 Domain，不是 Knowledge 子模块）：`src/modules/collection/`、`collection_sources`/`collection_tasks`，队列 `collection-fetch`，权限 `src/shared/collection-permissions.ts`。详见 `docs/collection/README.md`。
- 企业信息（普通名称/简称/Logo/简介/官网/资质）：`src/modules/company/`、`enterprise_profiles`/`enterprise_certificates`（历史字段保留）；路由 `/api/v1/platform/company` 与 `GET /api/v1/company/about`；权限复用 `system:md:enterprise:*`；详见 `docs/company/README.md`。
- 产品/材料参数主数据（Legacy，普通菜单已隐藏，表与 API 保留兼容）：`src/modules/masterdata/`、`md_*` 表；新功能禁止继续增加 Product 依赖。详见 `docs/masterdata/README.md`。
- 保温系统/构造方案/构造层/产品选项/方案文档：`src/modules/construction/`、`insulation_systems`/`construction_schemes`/`construction_layers`/`scheme_product_options`/`scheme_documents` 表；状态机与工作流工厂复用 masterdata（`registerVersionedEntity`/`registerVersionedWorkflow`），结构校验 `construction-structure.service.ts`，已发布读取 `construction-read.service.ts`，路由 `construction.routes.ts`（前缀 `/api/v1/platform/construction`），权限码 `src/shared/construction-permissions.ts`，导入示例 `pnpm construction:import-example`，详见 `docs/construction/README.md`。
- 图集热工参考选用表（参考集/参考行/Excel 导入作业/已发布查表）：`src/modules/thermal/`、`thermal_reference_sets`/`thermal_reference_rows`/`thermal_import_jobs`/`thermal_import_errors` 表；导入队列 `thermal-import`（`src/queues/queues.ts`、`src/workers/thermal-import.worker.ts`），apply 事务在 `thermal.service.ts`，解析匹配 `thermal-import.service.ts`，路由 `thermal.routes.ts`（前缀 `/api/v1/platform/thermal`），权限码 `src/shared/thermal-permissions.ts`，模板生成 `pnpm thermal:template`，详见 `docs/thermal/README.md`。
- 材料对比规则（VICP vs EPS/XPS/岩棉/聚氨酯/传统一体板的审核化对比规则、五维维度、批量导入、AI prompt 注入消费）：`src/modules/comparison/`、`comparison_versions`/`comparison_materials`/`comparison_dimensions`/`comparison_rules`/`comparison_evidence`/`ai_rule_usage_logs` 表；状态机与工作流工厂复用 masterdata，new-version 同事务复制子表并重映射 FK，结构校验 `collectComparisonViolations`，已发布读取 `comparison-read.service.ts`，prompt 上下文与使用日志 `material-compare.service.ts`，路由 `comparison.routes.ts`（前缀 `/api/v1/platform/comparison`）/`ai-comparison.routes.ts`（前缀 `/api/v1/ai/comparison`），权限码 `src/shared/comparison-permissions.ts`，错误码 `src/shared/comparison-errors.ts`，导入示例 `pnpm comparison:import-example`，详见 `docs/comparison/README.md`。
- Redis/BullMQ：队列定义、Fastify 插件、`src/worker.ts`。
- AI 服务商、模型、场景和提示词：AI 配置模块。快捷提问：`ai_quick_prompts`、`src/modules/ai/ai-quick-prompt.*`。
- AI 对话、围栏、能力路由、Agent Runtime、滚动摘要和检索：AI 模块、`ai-capability-router.ts`、`ai-agent.service.ts`、`ai-context-builder.ts`、知识模块、AI 参考规则。
- 项目 AI 长期记忆：`project_ai_memories`、`src/modules/ai/ai-project-memory.*`、路由 `/api/v1/projects/:id/ai-memories`。
- AI 回答点赞、反馈和重新生成：AI 模块、平台 AI 反馈模块、Drizzle schema。
- AI 聊天图片附件与 Vision 看图：`ai_message_attachments`、`src/modules/ai/ai-vision.service.ts`、文件 `purpose=CHAT_IMAGE`。
- 报告记录、来源、发布和导出：报告模块、报告 Worker、`report_sources`；普通生成按系统预置 `reportType` + `report_settings`，不传 templateId；`reports.projectId` 可空，`GET /api/v1/reports/my`。内部模板见 `docs/reports/README.md`。
- 公开分享：分享模块、`share_links`、`share_views`。
- 全局响应、错误和中文提示：共享响应/错误与错误处理插件。
- 长期业务或架构规则：同步更新 `AGENTS.md`、`README.md`、Codex skill 和 Cursor rules。
- 初始化权限、角色和动态菜单：`src/db/seed.ts`；新增后台权限码时必须同时更新 seed、对应路由守卫和权限说明。
- B/C/AI 客户端隔离：`src/plugins/auth.ts`、`src/shared/client-guard.ts`、`src/modules/auth/auth.routes.ts`。
- B 端按钮权限：`src/modules/system-management/system-management.routes.ts`、`src/modules/users/users.routes.ts`、`src/modules/platform-ops/platform-ops.routes.ts`、`src/modules/ai-config/ai-config.routes.ts`。
- AI 运营、反馈和审计权限：`src/modules/ai/ai-admin.routes.ts`、`src/modules/ai-feedback/ai-feedback.routes.ts`、`src/modules/audit-logs/audit-log.routes.ts`。
- 角色启用状态影响权限：`src/plugins/auth.ts`、`src/modules/menus/menu.service.ts`。
