---
description: AI 配置、围栏、知识检索、反馈、分享和报告规则
globs: src/modules/ai*/**/*.ts,src/modules/knowledge/**/*.ts,src/modules/reports/**/*.ts,src/modules/shares/**/*.ts,src/workers/report.worker.ts,src/db/schema.ts
alwaysApply: false
---

- 模型按场景读取数据库配置，禁止写死 DeepSeek、Base URL、模型 ID 或 API Key。C 端正式对话默认 `general_chat`，不要求用户选择 scene / prompt / quickPromptId；能力由 `resolveAiCapabilities` 按问题自动启用。
- 快捷提问使用独立表 `ai_quick_prompts`，不要复用 `prompts` / `prompt_versions`。点击后把 `content` 当作用户消息走现有 SSE。
- 基础 System Instruction 由 Backend 自动附加（`PLATFORM_BASE_SYSTEM_PROMPT`），用户和管理员日常流程不选择系统指令。场景/提示词菜单对普通业务管理员隐藏，接口保留给技术管理员。
- 知识库为「原文档导航 + 原始页面 + AI 检索索引」模型：ORIGINAL 展示原文件 / SEARCH_SOURCE 检索文本源（双源资产）；TOC=原文件目录（人工确认制，≠Section）；Page=物理页+印刷页码标签（pageLabel 非整数禁止 Number()）；转曲件 NO_TEXT_LAYER 走检索源映射（禁止物理页硬对齐）；parsedText 一律称「机器提取文本」，原文=ORIGINAL 页面/预览图。
- 重建只能删除 Section、Block、Chunk 等派生索引，必须保留 Page ID、页面预览、人工页签/标题、CONFIRMED TOC 和 MANUAL/verified 映射；双源重建读取 SEARCH_SOURCE，未映射检索页不得入 AI 索引或伪造 ORIGINAL 定位。生产 AI 仅检索当前、PUBLISHED、未过期、AI_ENABLED 的版本，BROWSE_ONLY 仅供浏览。B 端草稿验证走 `POST /platform/knowledge/versions/:versionId/test-qa`，从检索入口限定 versionId，不得全库检索后再过滤，也不得把该范围并入生产 `/ai/knowledge-qa`。
- AI 检索走 `searchWikiHierarchy`（章节/页面/内容块优先，Chunk 辅助），禁止默认全库 TopN chunk 拼接。正式聊天无项目时只检索平台已发布文档，有项目时平台 + 当前项目；不得检索草稿或其他项目私有资料。
- AI 来源契约统一走 `ai-source.mapper.ts`（AiSourceRef），正常生成与 regenerate 同构；C 端主要使用 `title` / `tocPath` / `sectionTitle` / `pageLabel` / `quote`；查看原文用 `originalFileId` + `physicalPageNumber`。无证据返回空数组，不伪造来源；来源详情走 `/api/v1/ai/knowledge/source-detail`。
- 专业 AI 会话必须选择保温体系（会话级 `insulationSystemId`，非 general_chat 强制），体系影响 Prompt、检索加权与候选查询。
- API Key 加密保存，任何响应不得返回密钥材料。
- AI 不执行任意 SQL、Shell、文件操作或绕过项目权限。
- AI 工具必须有 Zod 输入、权限检查和审计；工程计算必须由确定性服务完成。
- 技术回答引用检索资料；缺少可靠来源时用中文说明不确定。
- 保存会话、消息、模型、提示词版本、Token、耗时、失败、工具调用和检索来源。
- 流式回答必须支持 `POST /api/v1/ai/messages/:id/stop`；停止时保留部分内容、记录 `STOPPED` 状态和停止原因，会话仍可继续对话。
- 深度思考只按会话配置，默认 `OFF`，通过会话设置接口切换；消息保存实际 `reasoningMode`，历史配置不可被覆盖。
- 会话历史列表必须支持分页、搜索、来源和项目筛选，列表只返回摘要、最后消息预览和消息数量，不返回完整消息内容。
- 会话自动标题：首条用户消息回答完成后由 `ai-title-generation` 队列异步生成，仅当标题为空时写入，手动重命名后不覆盖；模型与提示词走 `conversation_title` 场景解析，生成失败保持“未知对话”。
- AI 对话敏感词围栏：发送消息前确定性校验 `ai_content_filters`（CONTAINS/REGEX，按场景生效），命中拦截并落库 `BLOCKED` + 审计，不发模型请求；B 端 `/api/v1/platform/ai/filters` 按 `system:ai:filter:*` 权限码管理。
- 会话软删除时保留审计和消息数据，并禁用该会话产生的 AI 分享链接；恢复不会自动恢复分享链接。
- 项目分享只保存项目摘要和已发布报告快照，不能包含源文件、知识库原文、未发布报告或原始 AI 会话。
- 点赞、反馈和重新生成不得覆盖原始 AI 回答；重新生成必须生成新消息并记录关系。
- 聊天图片走 `ai_message_attachments` + 文件中心 `CHAT_IMAGE`，不依赖 projectId；Vision 只看图，模型按 `capabilities.vision` / `code=default_vision` 解析。
- 会话无项目时跳过 projectContext，不阻断图片、检索、SSE 和报告生成。`reports.projectId` 可空；`GET /reports/my` 无项目时 `project=null`。
- 多条 AI 回答生成报告时，使用 `report_sources` 保存来源顺序和回答快照。
- AI 只生成结构化报告草稿，Worker 根据模板导出 HTML、图片、Word 和 PDF。
- 公开项目只允许查看已发布报告，原始 AI 会话保持私有。
- 公开分享只返回分享快照或报告文件临时下载地址，不暴露源文件、知识库原文或原始 AI 会话。
- 客户端隔离：`B_ADMIN` 才能访问 `/api/v1/platform/*` 和 `/api/v1/workspace/*`；C/AI 只能访问明确开放的 `/api/v1/ai/*` 业务能力，后台 AI 运营详情、反馈和配置必须使用独立 `system:ai:*` 权限码。
- 客户端访问令牌按客户端类型分别配置：`B_ADMIN` 默认 `24h`，`C_APP` 默认 `30d`，`PC_AI` 默认 `30d`；refresh token 统一默认有效 `30` 天。
- B 端接口必须执行精确权限码校验，禁止使用任意 `system:*` 作为兜底；禁用角色不再授予权限。AI 业务接口不依赖后台 RBAC，但必须执行客户端、项目、会话和文件归属校验。
- 用户端会话详情与后台运营详情分离：用户端仅看自己的消息、处理阶段、检索摘要、反馈、报告和分享；后台端可在有权限时查看工具调用、任务、分享访问和审计日志。两端都只展示中文处理阶段，不展示模型原始思考链。
